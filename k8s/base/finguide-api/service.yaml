apiVersion: v1
kind: Service
metadata:
  name: finguide-api
  labels:
    app.kubernetes.io/name: finguide-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finguide-api
  ports:
    - name: http
      port: 80
      targetPort: http
