apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: finguide-api-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finguide-api-postgres
  labels:
    app.kubernetes.io/name: finguide-api-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: finguide-api-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: finguide-api-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: finguide
            - name: POSTGRES_USER
              value: finguide
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: finguide-api-secrets
                  key: fg-api-db-password
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "finguide", "-d", "finguide"]
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: finguide-api-postgres-data
---
apiVersion: v1
kind: Service
metadata:
  name: finguide-api-postgres
spec:
  selector:
    app.kubernetes.io/name: finguide-api-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
