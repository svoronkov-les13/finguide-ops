apiVersion: v1
kind: Service
metadata:
  name: finguide-web
  labels:
    app.kubernetes.io/name: finguide-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finguide-web
  ports:
    - name: http
      port: 80
      targetPort: http
